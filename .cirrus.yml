container:
  image: archlinux:base-devel
  cpu: 8
  memory: 16G
  greedy: true
  use_in_memory_disk: true
  kvm: true
task:
  build_script:
      - chmod +x ./build.sh
      - ./build.sh
  upload_script:
      - chmod +x ./upload.sh
      - ./upload.sh

